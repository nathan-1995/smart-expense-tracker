commands:
  01_obtain_prod_certificate:
    command: |
      if [ ! -d "/etc/letsencrypt/live/api.fintracker.cc" ]; then
        certbot certonly --nginx -d api.fintracker.cc --non-interactive --agree-tos --email ndelima1@gmail.com
      fi
    ignoreErrors: true
